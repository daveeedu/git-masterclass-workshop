name: CI Placeholder

on:
  pull_request:
    branches: [main]

jobs:
  sanity-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Confirm repo checks out cleanly
        run: echo "✅ Files present and checkout succeeded — this stands in for a real test suite."